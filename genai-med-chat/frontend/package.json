{
  "name": "genai-med-chat-frontend",
  "version": "1.0.0",
  "private": true,
  "description": "GenAI Medical Chat Frontend - Lerna Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "setup": "npm install",
    "build": "lerna run build",
    "dev": "lerna run dev --parallel",
    "test": "lerna run test",
    "clean": "lerna clean",
    "clean:all": "bash ./clean_frontend.sh",
    "publish": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^7.4.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
